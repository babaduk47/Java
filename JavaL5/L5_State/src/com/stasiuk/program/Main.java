package com.stasiuk.program;

public class Main {

    public static void main(String[] args) {
        Task task = new Task();
        task.nextState();
        task.nextState();
        task.nextState();
        task.nextState();
        task.nextState();
        task.nextState();

        task.previousState();
        task.previousState();
        task.previousState();
        task.previousState();
        task.previousState();
        task.previousState();
    }
}
